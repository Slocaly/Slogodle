{
  "name": "slogodle",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.31.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start --filter=web",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.10.11"
  }
}
